module.exports = {
  root: true,
  extends: '@react-native',
  rules: {
    'semi': 'off', // Disable semicolon rule
    'quotes': 'off', // Disable single/double quotes rule
    'eol-last': 'off', // Disable the rule for enforcing a newline at the end of files
  },
};
